bin/
obj/

# Build output lands at the mod root because OutputPath=. in the csproj.
# The game loads Code.dll from here, but it's a build artefact — not in git.
Code.dll
Code.pdb
Code.deps.json
Code.runtimeconfig.json
Code.runtimeconfig.dev.json

# Project-level Claude Code settings can be committed (see .claude/settings.json)
# but local overrides aren't.
.claude/settings.local.json

# Agent worktrees — separate git repos that other Claude sessions
# create for isolated work. Never part of this repo's history.
.claude/worktrees/

# IDE
.vs/
.idea/
*.user
*.suo

# OS
Thumbs.db
.DS_Store
